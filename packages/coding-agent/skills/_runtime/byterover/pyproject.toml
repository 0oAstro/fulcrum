[project]
name = "fulcrum-byterover"
version = "0.1.0"
description = "Fulcrum Python facade for the optional ByteRover V4 Agent Skill"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/byterover"]
