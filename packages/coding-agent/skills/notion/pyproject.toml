[project]
name = "fulcrum-skill-notion"
version = "0.1.0"
description = "Notion integration skill for Fulcrum (Notion's official hosted MCP server)."
requires-python = ">=3.10"
dependencies = ["mcp", "httpx", "fulcrum-runtime"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/notion"]
