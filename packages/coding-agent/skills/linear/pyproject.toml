[project]
name = "fulcrum-skill-linear"
version = "0.1.0"
description = "Linear integration skill for Fulcrum (Linear's official MCP server)."
requires-python = ">=3.10"
dependencies = ["mcp", "httpx", "fulcrum-runtime"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/linear"]
